django-cloudcontrol-template
============================

The base template to be used inside cloudcontrol. This template comprends django + pipeline + various tweaks if you want to work with coffeescript + sass (compass) and django!
